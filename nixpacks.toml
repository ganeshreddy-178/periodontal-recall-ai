[phases.setup]
nixPkgs = ["python311", "python311Packages.pip", "gcc", "libglib", "libGL", "zlib", "libpng", "libjpeg"]

[phases.install]
cmds = [
    "pip install --upgrade pip",
    "pip install -r backend/requirements.txt"
]

[start]
cmd = "cd backend && bash start.sh"
